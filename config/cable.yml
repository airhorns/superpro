development:
  adapter: async

test:
  adapter: test

integration_test:
  adapter: async

production:
  adapter: redis
  url: <%= ENV.fetch("REDIS_URL") { "redis://localhost:6379/1" } %>
  channel_prefix: flurish_production
