---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: app-ingress
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/client-body-buffer-size: 2M
    nginx.ingress.kubernetes.io/proxy-body-size: 10M
spec:
  tls:
    - secretName: superpro.io-production
      hosts:
        - app.superpro.io
  rules:
    - host: app.superpro.io
      http:
        paths:
          - backend:
              serviceName: web
              servicePort: 80
---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: admin-ingress
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/client-body-buffer-size: 2M
    nginx.ingress.kubernetes.io/proxy-body-size: 10M
spec:
  tls:
    - secretName: admin.superpro.io-production
      hosts:
        - admin.superpro.io
  rules:
    - host: admin.superpro.io
      http:
        paths:
          - backend:
              serviceName: web
              servicePort: 80
