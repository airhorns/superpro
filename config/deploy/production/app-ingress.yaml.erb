---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: app-ingress
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/client-body-buffer-size: 2M
    nginx.ingress.kubernetes.io/proxy-body-size: 10M
spec:
  tls:
    - secretName: flurish.dev-production
      hosts:
        - app.flurish.dev
  rules:
    - host: app.flurish.dev
      http:
        paths:
          - backend:
              serviceName: web
              servicePort: 80
---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: admin-ingress
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/client-body-buffer-size: 2M
    nginx.ingress.kubernetes.io/proxy-body-size: 10M
spec:
  tls:
    - secretName: admin.flurish.dev-production
      hosts:
        - admin.flurish.dev
  rules:
    - host: admin.flurish.dev
      http:
        paths:
          - backend:
              serviceName: web
              servicePort: 80
